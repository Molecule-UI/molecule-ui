import { createUseStyles } from "react-jss";

const styles = createUseStyles({
  "@global": {
    html: {
      fontSize: "62.5%",
    },
    h4: {
      margin: 0,
      textAlign: "left",
    },
  },
});

export default styles;
