name: build

on: pull_request

jobs:
  run-build:
    runs-on: ubuntu-latest
    steps:
      - name: Install Dependencies
        run: npm install
      - name: build
        run: npm run build:components
