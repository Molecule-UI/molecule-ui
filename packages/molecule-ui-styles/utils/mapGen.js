const fs = require('fs');

// let base = 0;
// let count = 0;
// let inc = 0.25;
// let inc_10 = 0.5;
// const map = {};

// async function genreate(base, inc, count, map) {
//     for (base; base <= 20; base += inc) {
//         if (base < 10) {
//             map[count] = base;
//         } else {
//             inc = 0.5;
//             map[count] = base;
//         }
//         count++;
//     }
//     await fs.writeFileSync('./maps.js', JSON.stringify(map), { encoding: 'utf8' });
// }
// genreate(base, inc, count, map);
